spring:
  application:
    name: test-flow-scanner

blockchain:
  scanner:
    kafka:
      broker-replica-set: ${kafka.hosts}
    flow:
      sporks:
        - from: 50540412
          nodeUrl: "access.devnet.nodes.onflow.org"
